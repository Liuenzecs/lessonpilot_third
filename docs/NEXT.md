# 当前任务：Phase 12 Google Docs 手感的教师备课桌 UI 重构（已验收）

## 本阶段目标

只改 UI 和前端交互呈现，不改后端接口、数据库、生成逻辑和结构化 JSON 内容模型。把 LessonPilot 从“功能面板集合”重构为老师自己的在线备课文档桌：

- 公域页第一屏展示真实备课文档能力
- 备课台变成可判断下一步的备课队列
- 创建页变成课题优先的备课启动台
- 编辑器变成顶部工具栏 + 左侧目录 + 中间纸张画布 + 右侧检查栏
- 资料库、学校模板、旧教案导入同步文档桌视觉

## 已授权范围

- 不新增后端 API，不新增数据库表
- 不改变现有请求/响应结构、路由名称和业务流程
- 不做多人协同、评论线程、真实分页、自由格式 Word 处理器或复杂撤销历史
- 仍聚焦语文 MVP 和老师迁移体验
- 阶段结束停在验收口，不自动提交 / 推送，不进入下一阶段

## 实施清单

- [x] 写入 Phase 12 阶段文档与设计口径
- [x] 重置 `DESIGN.md / CLAUDE.md` 的 UI 方向为“教师文档桌”
- [x] 重构公域页首屏和产品表达
- [x] 重构备课台为“备课队列”
- [x] 重构创建页为“备课启动台”
- [x] 重构编辑器为文档画布三栏体验
- [x] 重构导入、学校模板、个人资料库页面视觉
- [x] 更新验收脚本和进度记录
- [x] 运行前端 type-check / test / build
- [x] 使用固定 `5173` 端口做本地浏览器验收

## 已实现

- `DESIGN.md` 已改为“教师文档桌”设计系统，`CLAUDE.md` 同步新的 UI 方向。
- 公域页首屏展示真实备课文档、目录、质量检查、引用和 Word 导出能力。
- 备课台改为“备课队列”，任务卡显示文档类型、课题、更新时间和下一步动作。
- 创建页改为“备课启动台”，课题输入成为主流程，右侧集中展示学校模板、个人资料库和知识增强提示。
- 编辑器改为顶部文档工具栏、左侧目录、中间纸张画布、右侧检查栏；学校模板、个人资料、质量体检和上课包进入右侧辅助区。
- 旧教案导入、学校模板和个人资料库沿用新的资料柜 / 格式库视觉。
- 编辑器样式拆分为 `editor.css` 与 `editor-fields.css`，保持单文件长度在项目约定内。

## 验证结果

- `pnpm --dir apps/web type-check`：passed
- `pnpm --dir apps/web test --run`：41 passed
- `pnpm --dir apps/web build`：passed
- `5173` 端口：已确认空闲后以 `--strictPort` 启动，当前监听进程为 `node`
- 浏览器验收：使用本机 Chrome headless 打开 `http://127.0.0.1:5173/`，确认公域首屏正常渲染真实文档桌预览

## 验收标准

- 新用户进入公域页，第一屏能看懂 LessonPilot 是“能生成、能检查、能导出学校格式 Word 的备课文档工具”
- 老师进入备课台后，能快速判断每份备课下一步该做什么
- 创建页不再像表单填报，课题输入和资料 / 模板推荐成为主流程
- 编辑器视觉上像在线文档：中间是可阅读的纸张，左边是目录，右边是质量、引用、导出和资料辅助
- 现有功能不退化：创建、生成、section 确认、AI 改写、RAG 引用、个人资料引用、质量修复、上课包、学校模板、Word 导出都能继续使用
- 页面没有明显文本溢出、控件重叠、卡片套卡片、过度装饰或一屏信息噪声

## 验收状态

Phase 12 已由用户验收通过。本轮按用户要求提交并推送后，等待下一步指示；不自动进入下一阶段。
